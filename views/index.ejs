<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        form {
            width: 510px;
            margin: 0 auto;
            border: 1px solid #ccc;
            padding: 20px;
            background-color: #f0f0f0;
            box-shadow: 0 0 10px 0 #ccc;
            transition: 0.3s;
            border-radius: 10px;
            background-image: linear-gradient(to bottom, #f0f0f0 0%, #e6e6e6 20%, #d4d4d4 40%, #c2c2c2 60%, #b0b0b0 80%, #9e9e9e 100%);
          }

          form:hover {
            box-shadow: 0 0 20px 0 #ccc;
            border-radius: 20px;
            background-image: linear-gradient(to bottom, #f0f0f0 0%, #e6e6e6 20%, #d4d4d4 40%, #c2c2c2 60%, #b0b0b0 80%, #9e9e9e 100%);
          }

          label {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 10px;
            color: #333;
          }

          input {
            width: 99%;
            padding: 10px;
            border: 1px solid #ccc;
            transition: 0.3s;
            border-radius: 10px;
            background-color: #fff;
          }

          input:hover {
            border-color: #000;
            border-radius: 20px;
          }

          button {
            width: 99%;
            border: none;
            background-color: #000;
            color: #fff;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
            border-radius: 10px;
            background-image: linear-gradient(to bottom, #000 0%, #111 20%, #222 40%, #333 60%, #444 80%, #555 100%);
          }

          button:hover {
            background-color: #ccc;
            border-radius: 20px;
            background-image: linear-gradient(to bottom, #000 0%, #111 20%, #222 40%, #333 60%, #444 80%, #555 100%);
          }

          @media (max-width: 768px) {
            form {
              width: 100%;
            }
          }
      </style>
</head>
<body>
    <% if(locals.fileLink!=null){%>
      <div style="margin-bottom: 1rem;">
        File Uploaded Successfully.
        <a href="<%= locals.fileLink %>"> <%= locals.fileLink %> </a> 
      </div>
    <% } %>
    <form action="/uploaded" method="post" enctype="multipart/form-data">
        <label for="file">File: </label>
        <input type="file" name="file" id="file" required>
        <label for="pass">Password: </label>
        <input type="password" name="pass" id="pass">
        <button type="submit">Upload</button>
    </form>
</body>
</html>